# Third-Party Notices

Cuemaster runtime dependencies are audited with:

```sh
npm run audit:licenses
```

Current runtime dependencies are expected to use permissive licenses. Dual-licensed packages are used under their permissive license option when one is available.

Development-only dependencies are not shipped with the app, but they should still avoid GPL-family or unclear licenses unless explicitly reviewed.
